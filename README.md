# Record-door-events
Record Door Events is a Home Assistant app that continuously buffers an RTSP camera stream, creates video clips when door events occur, and prepares them for sending to Telegram.
